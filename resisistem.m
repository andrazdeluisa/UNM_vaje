function [x] = resisistem(A, b)
    [L, U] = lubp(A);
    y = zeros(n);
    for i = 1:n
        %prema substitucija
    end
    x = zeros(n);
    for i = n:1:-1
        %obratna substitucija
    end

end

